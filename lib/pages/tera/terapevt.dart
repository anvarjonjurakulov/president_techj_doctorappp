import 'package:flutter/material.dart';

class Terapevt extends StatelessWidget {
  const Terapevt({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Patient"),
        centerTitle: true,
      ),
    );
  }
}