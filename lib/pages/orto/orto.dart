import 'package:flutter/material.dart';

class Ortoped extends StatelessWidget {
  const Ortoped({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Patient"),
        centerTitle: true,
      ),
    );
  }
}