import 'package:flutter/material.dart';

class Pediator extends StatelessWidget {
  const Pediator({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Patient"),
        centerTitle: true,
      ),
    );
  }
}