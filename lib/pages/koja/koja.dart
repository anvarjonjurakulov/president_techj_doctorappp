import 'package:flutter/material.dart';

class Koja extends StatelessWidget {
  const Koja({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Patient"),
        centerTitle: true,
      ),
    );
  }
}